<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>نظام المستشفى - الصفحة الرئيسية</title>
    <!-- Bootstrap RTL CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <!-- FontAwesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <!-- Cairo Font -->
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@300;400;500;600;700&display=swap" rel="stylesheet">

    <style>
        * {
            font-family: 'Cairo', sans-serif;
        }

        html, body {
            margin: 0;
            padding: 0;
            width: 100%;
            height: 100%;
            overflow: hidden;
        }

        .fullscreen-container {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
        }

        .bg-image {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            /* Fallback gradient instead of image */
            background-size: cover;
            filter: brightness(0.8);
        }

        /* Alternative with image overlay */
        .bg-image-with-photo {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(rgba(0,0,0,0.5), rgba(0,0,0,0.5)), url('https://images.unsplash.com/photo-1559757148-5c350d0d3c56?ixlib=rb-4.0.3&auto=format&fit=crop&w=1920&q=80');
            background-position: center center;
            background-repeat: no-repeat;
            background-size: cover;
        }

        .bg-content {
            position: relative;
            z-index: 2;
            height: 100%;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .masthead-heading {
            font-size: 4.5rem;
            color: white;
            text-shadow: 2px 2px 8px rgba(0, 0, 0, 0.7);
            font-weight: 700;
            margin-bottom: 1rem;
        }

        .masthead-subheading {
            font-size: 1.75rem;
            color: #f8f9fa;
            text-shadow: 1px 1px 5px rgba(0, 0, 0, 0.6);
            font-weight: 400;
            margin-bottom: 3rem;
        }

        .btn-custom {
            padding: 1.25rem 2.5rem;
            font-size: 1.1rem;
            font-weight: 600;
            letter-spacing: 1px;
            border-radius: 50px;
            background: linear-gradient(45deg, #28a745, #20c997);
            border: none;
            color: white;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
            transition: all 0.3s ease-in-out;
            text-decoration: none;
            display: inline-block;
        }

            .btn-custom:hover {
                transform: translateY(-3px);
                box-shadow: 0 7px 20px rgba(0, 0, 0, 0.3);
                background: linear-gradient(45deg, #20c997, #28a745);
                color: white;
            }

            .btn-custom:active {
                transform: translateY(-1px);
            }

        /* Responsive adjustments */
        @@media (max-width: 768px) {
            .masthead-heading

        {
            font-size: 2.5rem;
        }

        .masthead-subheading {
            font-size: 1.25rem;
        }

        .btn-custom {
            padding: 1rem 2rem;
            font-size: 1rem;
        }

        }

        /* Animation */
        .fade-in {
            animation: fadeIn 1s ease-in;
        }

        @@keyframes fadeIn {
            from

        {
            opacity: 0;
            transform: translateY(30px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }

        }

        .hospital-icon {
            font-size: 5rem;
            color: rgba(255, 255, 255, 0.9);
            margin-bottom: 2rem;
            text-shadow: 2px 2px 8px rgba(0, 0, 0, 0.5);
        }
    </style>
</head>
<body>
    <div class="fullscreen-container">
        <!-- Use bg-image for gradient or bg-image-with-photo for photo background -->
        <div class="bg-image"></div>

        <header class="bg-content">
            <div class="container px-4 px-lg-5 text-center fade-in">
                <div class="hospital-icon">
                    <i class="fas fa-hospital"></i>
                </div>

                <h1 class="masthead-heading">نظام المستشفى</h1>
                <h3 class="masthead-subheading">
                    <em>أهلاً بك في نظام الإدارة المتكامل</em>
                </h3>

                <a href="/Hospital/CreateTicket" class="btn btn-custom">
                    <i class="fas fa-plus me-2"></i>
                    إنشاء تذكرة جديدة
                </a>
            </div>
        </header>
    </div>

    <!-- Bootstrap Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>